<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%=link_to "SPINNER", albums_path, {class: "navbar-brand"} %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active">
          <% if current_user %>
            <% if current_user.name %>
              <%= link_to "Hello, #{current_user.name}", user_path(current_user) %><span class="sr-only">(current)</span>
            <% else %>
              <%= link_to current_user.email, user_path(current_user) %>
            <% end %>
          <% else %>
            <%= link_to "Log In", signin_path %>
          <% end %>
        </li>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="true">Get me outta here<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><% if current_user %>
                  <%= link_to "Edit my profile", edit_user_path(current_user) %>
                <% else %>
                  <%= link_to "Sign me up!", new_user_registration_path%>
                  <% end %></li>
            <li><%= link_to "Show me everything", albums_path%></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Log me out", signout_path%></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
